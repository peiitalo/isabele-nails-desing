# Porta do servidor
PORT=3333

# Secret para gerar tokens JWT (NÃO compartilhe e NÃO use em produção a mesma chave)
JWT_SECRET=isa-nails-secret-key-change-in-production

# Node environment
NODE_ENV=development
